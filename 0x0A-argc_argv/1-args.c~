#include <stdio.h>

/**
 * main - prints the for the program.
 * @argc: is a counter of arguement for int.
 * @argv: is a counter for value of the arguement to char
 * Return: Returns always success.
 **/

int main(int argc, char *argv[])
{

  (void)))